"""Performance test harness for the Feldera Solace Platform connector.

Run with ``python -m solace_perf --help`` (or ``./run.sh --help``, which also
brings up the docker-compose stack). See README.md for methodology.
"""
